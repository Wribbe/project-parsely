#include "bus.h"

#define MAX_CONSUMERS 256

struct bus_event CONSUMERS[MAX_CONSUMERS];

int
bus_register(bus_callback_function fn)
{
  return -1;
}

int
bus_add(struct bus_event event)
{
  return -1;
}
